# Neural-Density-Controllers
 In this work, we address these challenges by proposing a complementary view of neural control synthesis: Neural Density Controllers, wherein we learn a controller that is a ratio of two neural networks. The functions constituting the controller satisfy the density stability conditions proposed by Rantzer.
